export const metadata = {
    title: 'Credentials',
};

export default function AuthPages({ children }) {
    return (
        <main>
            {/* <Navbar /> */}
            {children}
        </main>
    );
}
